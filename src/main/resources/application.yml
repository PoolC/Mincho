server:
  address: localhost
  port: 8080
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password:
    url: jdbc:postgresql://3.35.170.34:5432/poolc?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug